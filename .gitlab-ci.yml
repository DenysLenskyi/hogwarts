services:
 - name: docker:dind
   command: ["--tls=false"]
variables:
 DOCKER_HOST: "tcp://docker:2375"
 DOCKER_TLS_CERTDIR: ""
 DOCKER_DRIVER: overlay2

stages:
  - compile
  - test
  - build

maven-compile-job:
  image: maven:latest
  stage: compile
  script:
    - mvn compile

maven-test-job:
  image: maven:latest
  stage: test
  script:
    - mvn test

maven-build-job:
  image: maven:latest
  stage: build
  script:
    - mvn package
  artifacts:
    paths:
      - target/*.jar